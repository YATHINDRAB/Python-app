flask
install:
  runtime-versions:
    python: 3.11
  commands:
    - echo "Installing dependencies..."
    - pip install -r Python-app/Requirements.txt 

pre_build:
  commands:
    - echo "Installing dependencies..."
    - pip install -r Python-app/Requirements.txt --retries 5 --timeout 30 


pre_build:
  commands:
    - python -m venv venv
    - source venv/bin/activate
    - echo "Installing dependencies..."
    - pip install -r Python-app/Requirements.txt


